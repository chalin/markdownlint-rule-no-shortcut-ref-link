name: publish

# Auth: npm trusted publishing (OIDC). Requires one-time setup on npmjs.com
# (package Settings > Trusted Publisher: this repo + this workflow + the
# npm-publish environment).

on:
  release:
    types: [published]

permissions: {}

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm-publish
    permissions:
      contents: read
      id-token: write # OIDC trusted publishing + provenance
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
      - name: Check that the release tag matches the package version
        run: |
          version=$(node -p 'require("./package.json").version')
          test "$GITHUB_REF_NAME" = "v$version"
      # No install or checks: `test` already gates every push, and publishing
      # needs no node_modules, so no registry-delivered code runs with
      # id-token authority.
      - run: npm publish --ignore-scripts
